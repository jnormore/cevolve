# =============================================================================
# AGENT CONFIGURATION
# =============================================================================

# MODEL IS FIXED - DO NOT CHANGE
# The goal is prompt optimization, not model selection
model: qwen2.5:1.5b

# =============================================================================
# TUNABLE PARAMETERS - Evolve can modify these
# =============================================================================

# Maximum iterations (tool uses) before giving up
max_iterations: 3

# Include full conversation history in context
include_history: false
